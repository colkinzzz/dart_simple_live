# Simple Live 车机适配版

这个分支面向允许第三方应用在右侧约 2/3 区域运行的 Android 车机。

## 行为变化

- “播放器全屏”只填满应用当前窗口，不再请求横屏，也不会主动退出车机分屏。
- 车机模式下播放器全屏（包括双击）只改变当前应用窗口内的布局，不请求改变车机宿主窗口或横竖屏；只有原生检测可靠确认宿主已经整屏时才隐藏系统栏。
- 应用背景通过透明状态栏延伸到车机顶部，交互控件仍由安全区保护。
- 车机模式使用系统真实安全区域，不再把超过 50dp 的顶部区域强制压缩为 25dp。
- 直播间布局根据当前应用窗口大小选择布局，不再仅凭屏幕方向判断平板模式。
- 在“设置 → 直播间设置 → 车机适配”中可以调整额外上下安全区。

车机模式把三个状态分开处理：车机宿主窗口（分屏/整屏/未知）、播放器布局（普通/应用内全屏）和系统栏（显示/沉浸）。播放器全屏只改变当前应用布局；只有原生窗口检测可靠确认宿主已经整屏时才隐藏系统栏，分屏或未知状态始终保留车机系统栏。

窗口检测只把 DisplayManager 枚举到的 `TYPE_BUILT_IN` 显示屏作为物理基准；车机为分屏创建的 `TYPE_VIRTUAL`、overlay 或 Wi-Fi 显示屏即使自身报告了 Display.Mode，也不会授权沉浸。无法唯一匹配真实物理屏时按未知状态处理。

## 建议测试顺序

1. 保持车机右侧 2/3 分屏，打开直播间并点击播放器全屏；车机不应退出分屏。
2. 检查顶部返回键、标题和底部播放器按钮是否都能点击。
3. 如果厂商系统没有完整上报安全区域，按 4dp 逐步增加顶部或底部额外安全区。
4. 由车机系统手动切换整屏或分屏，验证播放器全屏不会反向改变车机窗口状态；如果窗口状态无法可靠确认，应用会选择保守地显示系统栏。

车机状态栏与空调控制条始终由车机系统控制；应用只保留安全区域，避免控件被系统层覆盖。

车机构建工作流会优先使用 `KEYSTORE_BASE64`、`STORE_PASSWORD`、`KEY_PASSWORD` 和 `KEY_ALIAS` Secrets。未配置时仍可使用临时测试签名构建，但不同运行生成的 APK 不能互相覆盖升级。

